name: Check

on:
  push:
    branches:
      - '**'

jobs:
  format:
    runs-on: ubuntu-latest
    name: Format Check
    steps:
      - name: Check out repository
        uses: actions/checkout@v2

      - name: Set up Node
        uses: actions/setup-node@v3
        with:
          node-version: 18
          cache: 'npm'

      - name: Install dependencies
        run: npm i
        working-directory: ./trivia-basket-frontend

      - name: Check formatting
        run: npm run format:check
        working-directory: ./trivia-basket-frontend

  lint:
    runs-on: ubuntu-latest
    name: Lint
    steps:
      - name: Check out repository
        uses: actions/checkout@v2

      - name: Set up Node
        uses: actions/setup-node@v3
        with:
          node-version: 18
          cache: 'npm'

      - name: Install dependencies
        run: npm i
        working-directory: ./trivia-basket-frontend

      - name: Run linter
        run: npm run lint
        working-directory: ./trivia-basket-frontend

  build:
    runs-on: ubuntu-latest
    name: Build
    steps:
      - name: Check out repository
        uses: actions/checkout@v2

      - name: Set up Node
        uses: actions/setup-node@v3
        with:
          node-version: 18
          cache: 'npm'

      - name: Install dependencies
        run: npm i
        working-directory: ./trivia-basket-frontend

      - name: Run build
        run: npm run build
        working-directory: ./trivia-basket-frontend
